export interface pokeDTO {
  name: string;
  url: string;
}
[];

export interface pokeDetailsDTO {
  name: string;
  url: string;
  height: number;
  weight: number;
  sprites: {
    front_default: string;
  };
}
[];
